# Option Design System

Every value must be exact enough to implement without visual guesswork. Create one completed copy for each proposed option and a coded selected version after human approval.

## Direction

- Audience and product character:
- Visual principles:
- Explicit anti-goals:
- Mockup references:

## Semantic Color

| Token | Exact value | Use | Contrast or state rule |
| --- | --- | --- | --- |
| `color.background.canvas` |  |  |  |
| `color.background.surface` |  |  |  |
| `color.text.primary` |  |  |  |
| `color.text.secondary` |  |  |  |
| `color.border.default` |  |  |  |
| `color.action.primary` |  |  |  |
| `color.state.success` |  |  |  |
| `color.state.warning` |  |  |  |
| `color.state.danger` |  |  |  |

Define hover, active, focus, selected, disabled, loading, error, dark/high-contrast, and non-color cues where applicable.

## Typography

| Role | Family/fallback | Weight | Size | Line height | Letter spacing |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  | `0` |

## Geometry And Layout

- Spacing scale:
- Radius scale, maximum card radius 8px unless the established system requires otherwise:
- Border and elevation values:
- Content widths and grid tracks:
- Breakpoints and container behavior:
- Stable dimensions and aspect ratios:
- Density and touch-target rules:

## Components

For every component, define anatomy, exact dimensions, variants, content limits, responsive behavior, and default, hover, active, focused, selected, disabled, loading, empty, error, destructive, and permission-denied states as applicable.

| Component | Variants and states | Token references | Gallery story/test |
| --- | --- | --- | --- |
|  |  |  |  |

Use familiar icons in tool buttons with accessible names and tooltips where needed. Avoid text-filled rounded controls when a standard symbol communicates the command more clearly.

## Motion And Accessibility

- Durations and easing:
- Reduced-motion behavior:
- Focus order and focus appearance:
- Keyboard and assistive-technology behavior:
- Contrast targets and zoom/reflow behavior:

## Assets

| Asset ID | File | Exact dimensions | Intended use | Generation/provenance | Status |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  | proposed |

## Fidelity Acceptance

- Component gallery path and command:
- Reference viewport and capture command:
- Pixel/visual tolerance and review method:
- Human-approved exceptions:
