# Agent Workflows

`skills/` is this repository's canonical skill source. `.agents/skills/`, `.claude/skills/`, and `.codex/skills/` are generated discovery copies; do not edit them directly.

After adding or changing a canonical skill, run:

```bash
npm run project:skills
npm run project:check
```
