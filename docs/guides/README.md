# Guides

`developer.md` owns setup and environment behavior. Add `user.md`, `operator.md`, or release instructions only after the corresponding behavior exists and has a stable audience. Keep commands in `docs/commands.md` and link to them rather than duplicating command blocks.
