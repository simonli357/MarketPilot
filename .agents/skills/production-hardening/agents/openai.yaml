interface:
  display_name: "Production Hardening"
  short_description: "Harden one block against explicit risks"
  default_prompt: "Use $production-hardening to define and satisfy strict criteria for the active block."
policy:
  allow_implicit_invocation: false
